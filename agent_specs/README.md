# Agent Specs

Canonical source of truth for the new Agent OS.

Each `*.yaml` file in this directory defines one agent spec. The Agent OS runtime only loads generated active agents derived from validated specs.
